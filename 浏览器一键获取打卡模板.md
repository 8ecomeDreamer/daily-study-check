let d = new Date();
// 格式化日期 YYYY-MM-DD
const date = d.getFullYear() + "-" + String(d.getMonth()+1).padStart(2,0) + "-" + String(d.getDate()).padStart(2,0);
// 当前时分
const nowTime = String(d.getHours()).padStart(2,0) + ":" + String(d.getMinutes()).padStart(2,0);

// 距离今年剩余天数
const endYear = new Date(d.getFullYear(), 11, 31);
const leftYearDays = Math.ceil((endYear - d) / (1000*60*60*24));
// 距离2027春节(2027-02-06)
const springDay = new Date(2027, 1, 6);
const leftSpringDays = Math.ceil((springDay - d) / (1000*60*60*24));

// 只声明一次模板变量，不会冲突
const dailyTemplate = `# 📅 每日打卡 | ${date}
节奏：工作日加练 | 休息日3h加练

🔥 **本月核心目标**：

🔥 **本周目标**：

⏳ 距离今年结束：${leftYearDays} 天
🧨 距离2027新年春节：${leftSpringDays} 天

⏰ 学习开始：${nowTime}
⏰ 学习结束：

---

📝 备忘录
> 可以是往日盲区也可以是todo，推荐按实际情况填顺序
-

---

## 🎒 今日工作亮点（核心重点）
> 上班解决的问题、学到的技术，直接变成面试项目话术
- **今日掌握/踩坑技术点**：
- **业务解决问题**：
- **封装/沉淀包装思路（简历可用）**：

---
## ✅ 今日加练（碎片化学习）
- 

---
## ❓ 遗留盲区（查缺补漏）
- 

---
## 🎯 明日计划（一句话收尾）
-

  `;

// 输出模板到控制台
console.log(dailyTemplate);